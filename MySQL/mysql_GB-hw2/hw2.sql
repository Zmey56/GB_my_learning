CREATE DATABASE IF NOT EXISTS example;

USE example;

CREATE TABLE IF NOT EXISTS users (
  id INT,
  name CHAR(10)
);

CREATE DATABASE IF NOT EXISTS sample;


# mysqldump example > exampl_dum.sql

# mysql sample < exampl_dum.sql


 